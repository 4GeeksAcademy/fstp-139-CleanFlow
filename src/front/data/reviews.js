/**
 * VALORACIÓN MEDIA DE CLIENTES.
 *
 * ⚠️ PROVISIONAL: la tabla de reseñas todavía no existe, así que el dato
 * está escrito a mano.
 *
 * Cuando exista: se crea services/reviewService.js con
 * getReviewsSummary(), igual que serviceService.js, se guarda en el store
 * y este archivo se borra.
 *
 * El número vive aquí y no dentro del componente: cambiarlo es tocar una
 * línea, no buscarlo por medio proyecto.
 */

export const REVIEWS_SUMMARY = {
    average: 4.8,   // de 0 a 5
    total: 128,     // opiniones contadas
}
